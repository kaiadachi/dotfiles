# dotfiles
* fish
* git
* tmux

## Setup

# brew
$ bash bin/brew-init.sh # use homebrew

# apt
$ bash bin/apt-init.sh  # use ubuntu

# usage
$ make # full install
$ make server # server config install

